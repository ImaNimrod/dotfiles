#!/bin/sh

set -euo pipefail

action=$(echo -e "Lock\nReboot\nShutdown" | dmenu -c -i -l 3 -p "Choose an action:")
case "$action" in
    "Lock")
        slock
        ;;
    "Reboot")
        sudo -A reboot
        ;;
    "Shutdown")
        sudo -A poweroff
        ;;
esac
